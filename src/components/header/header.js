import React from 'react';

export function Header() {
  return (
    <nav>
        <div class="navbar">
            <button>Home</button>
            <button>About Me</button>
            <button>Contact</button>
        </div>
    </nav>
  );
}
